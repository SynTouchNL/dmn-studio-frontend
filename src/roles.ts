const prefix = 'dmnstudio_'

export const ROLES = {
    READ: `${prefix}read`,
    DEPLOYER: `${prefix}deployer`,
    ADMIN: `${prefix}admin`,
    DEVELOPER: `${prefix}developer`,
    APPROVER: `${prefix}approver`
};